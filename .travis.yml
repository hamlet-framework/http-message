language: php
sudo: false

os:
  - linux

php:
  - '7.0'
  - '7.1'
  - '7.2'
  - '7.3'

before_script:
  - composer clear-cache
  - composer update

script:
  - ./vendor/bin/phpunit --testdox
  - ./vendor/bin/phpcs --standard=PSR2 -n src
  - ./vendor/bin/psalm
